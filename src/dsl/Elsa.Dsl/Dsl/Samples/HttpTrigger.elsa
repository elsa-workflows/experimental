// Triggers when HTTP request comes in.
trigger HttpTrigger
{
    Path: "/hello-world",
    SupportedMethods: ['GET']
}

// Triggers every 1 minute.
trigger Timer
{
    Timeout: "0:00:01:00"
}

WriteLine("Hello World!");